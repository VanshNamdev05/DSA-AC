#include<iostream>
using namespace std;

int main(){
    cout << (int)('A') << endl;
    // explicit -> when we convert the type, typecasting to another datatype

    float PI = 3.14;
    cout << (int)(PI) << endl;

    cout << ((float)10/3) << endl;

    cout << (char)('A' + 1) << endl;
}