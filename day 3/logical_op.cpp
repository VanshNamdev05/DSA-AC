#include <iostream>
using namespace std;

int main()
{
    // AND - &&
    // OR -> ||
    // NOT -> !

    cout << ((3 < 5) && (10 > 5)) << endl; // 1(True)
    cout << ((3 < 5) && (10 < 5)) << endl; // 0(False)
}